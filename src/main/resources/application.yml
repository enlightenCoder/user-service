server:
  port: 9092

spring:
  profiles:
    active: dev
  application:
    name: USER-MS
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    show-sql: true
    hibernate:
      ddl-auto: create
